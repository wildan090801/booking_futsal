import 'package:flutter/material.dart';

class ManageFieldScreen extends StatelessWidget {
  const ManageFieldScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
